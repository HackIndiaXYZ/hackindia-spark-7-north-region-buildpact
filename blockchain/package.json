{
  "name": "buildpact-contracts",
  "version": "1.0.0",
  "description": "BuildPact escrow and reputation NFT smart contracts — Polygon Amoy testnet",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "node": "hardhat node",
    "deploy:local":  "hardhat run scripts/deploy.js    --network hardhat",
    "deploy:amoy":   "hardhat run scripts/deploy.js    --network amoy",
    "deployNFT:local": "hardhat run scripts/deployNFT.js --network hardhat",
    "deployNFT:amoy":  "hardhat run scripts/deployNFT.js --network amoy",
    "clean": "hardhat clean"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "dotenv": "^16.6.1",
    "hardhat": "^2.22.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "5.0.2"
  }
}
